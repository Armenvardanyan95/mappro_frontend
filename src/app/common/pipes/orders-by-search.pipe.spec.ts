import { OrdersBySearchPipe } from './orders-by-search.pipe';

describe('OrdersBySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdersBySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
